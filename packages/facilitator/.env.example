# x402 Facilitator Configuration

# Server port
PORT=4020

# JWT secret for signing receipts (generate a strong random string)
JWT_SECRET=change-me-to-a-random-secret

# Fee percentage (e.g., 2 = 2%)
FEE_PERCENT=2

# Self URL (used in receipt metadata)
FACILITATOR_URL=http://localhost:4020

# Use mock transfers (no real on-chain execution)
MOCK_TRANSFERS=true

# --- Future: Real on-chain execution ---
# FACILITATOR_PRIVATE_KEY=0x...
# RPC_URL=https://mainnet.base.org
